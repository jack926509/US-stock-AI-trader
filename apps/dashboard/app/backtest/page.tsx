export default function BacktestPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold">回測績效</h2>
      <p className="mt-2 text-sm text-gray-500">Phase 3 將實作：以 SQL 對 trade_decisions + positions 重放，算勝率、PF、最大回撤。</p>
    </div>
  );
}
